# 括号中为空即是空元组
()

# 元组中只有一个元素时，该元素后面要带有逗号,如果没有逗号，不会被认为是元组
# 输出5*2 = 10
a = (5)
print(a * 2)
# 输出元组5，扩大2长度，改为2个5的元组(5, 5)
b = (5,)
print(b * 2)

# 元组查询：可以使用下标
c = (1, 2, 3, 14, 5)
print(c[0])

# 元组排序：排序结果为list，需要强转为tuple
print(tuple(sorted(c)))